
public function main(): int32 
{
    'test: { // not all path expresses the value
        if (true) { express 'test 0; }
    }

    'test: { // all path expresses the value
        if (true) { express 'test 1; }
        express 2u32;
    }
}
