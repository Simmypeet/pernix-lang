public enum SomeEnum {
    First,
    Second
}

public function main(mutable someParameter: int32): void {
    let value = 32;

    someParameter;
    value;
    SomeEnum; // Error - cannot access enum type directly
    SomeEnum::First;

    // shadow the parameter
    let mutable someParameter = 32;

    someParameter = 5i64;
}
