public struct SomeStruct 
{
public:
    type A = float32; 
    type B = A;       

    let first: A;         // refers to ::test::SomeStruct::A;
    let second: B;        // refers to ::test::SomeStruct::B;
    let third: ::test::A; // refers to ::test::A
}

public type A = float64;
public type B = C;
public type C = B;

public enum Days {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
}