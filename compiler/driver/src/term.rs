//! Contains the configuration for terminal display using miette.
//!
//! This module is kept for compatibility but miette handles theming
//! automatically via the `GraphicalReportHandler`.

// Note: miette uses GraphicalTheme for styling. The default themes
// (unicode, unicode_nocolor, ascii) are used directly in the code.
// No additional configuration is needed as miette handles this automatically.
