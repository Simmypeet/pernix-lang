pub struct MyPair[T, U]:
    pub first: T
    pub second: U


pub def print[T](value: T):
    pass


pub def main():
    let mut outer = 32

    scope: 
        let mut inner = 64

        let pair = MyPair {
            first: &outer,
            second: &inner,
        }

        outer = 32

        print(*pair.second)