pub struct RefWrapper['a, T]:
    where:
        T: 'a

    pub ref: &'a T


pub def test['a, 'b](
    ref: &'a int32
) -> RefWrapper['b, int32]:
    let test = 32
    let another = &test

    return RefWrapper { ref: ref }