pub struct MyPair['a, 'b, T, U]:
    where:
        T: 'a
        U: 'b
        'a: 'b # 'a flows into 'b

    pub first: &'a T
    pub second: &'b U


pub def print[T](value: T):
    pass


pub def main():
    let mut outer = 32

    scope:
        let mut inner = 64

        let pair = MyPair {
            first: &outer,
            second: &inner,
        }

        outer = 32

        # since 'first' flows into 'second', this will error
        print(*pair.second)