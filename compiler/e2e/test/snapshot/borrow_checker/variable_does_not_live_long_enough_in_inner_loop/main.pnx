public function test(cond: bool):
    let outer = 1
    let mut numberRef = &outer

    while cond:
        let inner = 0
        numberRef = &inner

        while cond:
            let innerInner = 0
            numberRef = &innerInner


    let a = *numberRef