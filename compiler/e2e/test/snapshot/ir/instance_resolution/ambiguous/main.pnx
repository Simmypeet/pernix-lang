public trait Order[T]:
    pass


public struct MaxHeap[T, inst O: Order[T]]:
    pass



public function requireOrder[T, inst O: Order[T]](value: T):
    pass


implements[T, inst O: Order[T]] MaxHeap[T, O]:
    public function withOrder[inst O2: Order[T]](value: T):
        # both `O` and `O2` are valid instances for `Order[T]`, so this should 
        # cause an ambiguous inst resolution error
        requireOrder(value)