pub marker Test[T]


pub struct Another[T]:
    priv value: T


impl[T] Another[T]:
    pub def requireTest(self: &this):
        where:
            T: Test


    pub def useTest(self: &this):
        # should invoke `T: Test` unsatisfied
        self.requireTest()