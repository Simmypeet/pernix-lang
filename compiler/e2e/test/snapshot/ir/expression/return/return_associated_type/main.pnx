pub trait Fizz[T]:
    pub type Output


    pub def fizz(x: T) -> this::Output


pub inst Test[T]: Fizz[T]:
    type Output = T


    def fizz(x: T) -> this::Output:
        if true:
            return x # should be fine
        else:
            return 32 # should be a type error