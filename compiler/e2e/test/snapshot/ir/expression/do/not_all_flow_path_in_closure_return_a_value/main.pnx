pub effect Test:
    op()


# no error here
pub def main():
    let cond = true

    do:
        if cond:
            return ()
    
    with Test:
        op():
            pass


# error here, not all flow paths return a value
pub def main2():
    let cond = true

    do:
        return true
    
    with Test:
        op():
            if cond:
                return false
            
            # missing else branch, not all flow paths return a value