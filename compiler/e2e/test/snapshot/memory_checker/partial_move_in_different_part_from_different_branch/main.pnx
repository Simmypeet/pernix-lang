pub def consume[T](x: T):
    pass


pub def partialMove[T](x: (T, T, T, T), cond: bool):
    match (cond, cond):
        (true, false): 
            consume(x.0)
        
        (false, false):
            consume(x.1)
        
        (.., true):
            consume(x.2)
        
    consume(x.3)
