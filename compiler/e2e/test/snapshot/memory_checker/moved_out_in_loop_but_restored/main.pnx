pub def create[T]() -> T:
    panic


pub def consume[T](x: T):
    pass


pub def test[T](mut x: (T, T), cond: bool):
    while cond:
        consume(x)

        x.0 = create()
        x.1 = create()