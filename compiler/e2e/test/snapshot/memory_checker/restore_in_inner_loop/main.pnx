pub def create[T]() -> T:
    panic


pub def consume[T](x: T):
    pass


pub def main[T](mut a: T):
    while true:
        # will be consumed
        consume(a)

        loop:
            # but always restored before the while iteration
            
            a = create()

            if true:
                break
