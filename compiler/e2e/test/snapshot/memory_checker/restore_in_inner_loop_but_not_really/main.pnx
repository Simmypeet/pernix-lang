
pub def create[T]() -> T:
    panic


pub def consume[T](x: T):
    pass


pub def main[T](mut a: T, cond: bool):
    while cond:
        # will be consumed
        consume(a)

        loop:
            if cond:
                break

            # but always restored before the while iteration
            a = create()
