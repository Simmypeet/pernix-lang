public type First['x, T, 'a] = &'x T:
    where:
        T: 'x

public type Second[const N: usize, 'a] = int32

public type Third[T, const N: usize, 'a] = float32

public type Fourth[const N: usize, T] = bool
