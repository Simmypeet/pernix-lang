## Case
## 1 2 3


extern "C":
    pub def scanf(format: &uint8, ...) -> int32
    pub def exit(code: int32)


pub def add[T: tuple](a: T) -> (int32, ...T, int32):
    return (1, ...a, 5)


pub def main():
    unsafe scope:
        let mut nums = (0i32, 0i32, 0i32)

        scanf(&"%d %d %d\0".[0], 
            &mut nums.0, 
            &mut nums.1, 
            &mut nums.2
        )

        let test = add(nums)

        if (test.0 == 1 
            and test.1 == nums.0 
            and test.2 == nums.1 
            and test.3 == nums.2 
            and test.4 == 5
        ):
            exit(0)

        else:
            exit(1)