[toolchain]
# currently, the latest nightly version as of "2026-01-25" has a subtle bug
# with MacOS 26 Arm64 builds causing memory allocation failures.
# See https://github.com/rust-lang/rust/issues/150898
channel = "nightly-2026-04-18"
components = ["clippy", "rustfmt"]
